# 疑問点（設計書に基づく検証・整理）

**参照する最新版**: [review_observation_SSOT設計書_20260302_rev2.md](../review_observation_SSOT設計書_20260302_rev2.md)（確定版 rev2・全指摘統合済）

本文書は上記設計書を「無批判に信用せず確認した」結果を構造化してまとめたものです。  
rev2 で rev1 の欠落項目（テスト配置 / raw 保存単位 / alert_type / Cloud Run 認証 等）を復元・統合したため、従前の疑問点はすべて設計書内で明文化され解消済みとします。

---

## 構成

1. **rev2 で解消済みの項目** … 疑問点指摘に対し rev2 で確定した内容（参照箇所は rev2 の節番号）
2. **一覧** … 解消状況のサマリ
3. **今後の確認の目安** … 実装・運用で新たな疑問が出た場合の扱い

---

# 1. rev2 で解消済みの項目

設計書 rev2 の記述により、以下の疑問はすべて解消済みとする。

## 1.1 認証・公開範囲

| 旧疑問点 | rev2 での確定内容 | 参照箇所 |
|----------|-------------------|----------|
| GBP OAuth 用 Secret の実体 | Secret は **JSON 1本**（client_id, client_secret, refresh_token）。ENV は **GBP_OAUTH_SECRET_NAME** | §2.1 |
| Cloud Run 認証・Scheduler 呼び出し | **未認証アクセス禁止**。Scheduler は **OIDC**。`run.invoker` は Scheduler 用 SA のみ | §2.2 |

## 1.2 テーブル・運用

| 旧疑問点 | rev2 での確定内容 | 参照箇所 |
|----------|-------------------|----------|
| places_provider_map の初期データ | **手動INSERT**（README/infra に手順記載）。任意列に provider_account_id, display_name | §3.1 |
| raw_provider_payloads の保存単位 | **店舗（store_code）× provider × endpoint ごとに 1 行**。endpoint 列あり | §3.4 |
| reviews のパーティション・重複排除 | PARTITION BY DATE(ingested_at)。重複排除は provider_review_id | §3.3 |

## 1.3 SQL・VIEW・Sheets

| 旧疑問点 | rev2 での確定内容 | 参照箇所 |
|----------|-------------------|----------|
| 010_merge_snapshot.sql の役割 | **雛形（ドキュメント）**。本番実行は **bq_writer.py** が実施。001/002 は手動適用 | §9 |
| v_latest と LATEST の対応 | **v_latest_with_delta_ratings** と **v_latest_with_delta_performance**。VIEW 参照 → Sheets 全置換 | §5.1, §5.2, §6 |
| v_rating_alerts の alert_type | 固定値: **low_rating / rating_drop / review_surge**。閾値は SQL 固定 | §5.3 |
| LATEST に fetched_at | **含める**（確定と明記） | §7.1 |
| performance_monthly の条件 | 月次は **毎月1日 09:00 JST の Scheduler**（別ジョブ推奨）。月初ジョブで MERGE | §4.2, §6 |

## 1.4 出力・環境変数・テスト・CI

| 旧疑問点 | rev2 での確定内容 | 参照箇所 |
|----------|-------------------|----------|
| CSV 用 GCS bucket | **GCS_EXPORT_BUCKET**（ENV・必須）。出力先例を列挙 | §8 |
| テストの配置 | テストは **tests/ 配下**。CI では **pytest tests** を実行 | §10 |
| CI/CD・ヘルスチェック | ruff / pytest（tests/）。Deploy 後 **GET /health** で確認 | §11 |

---

# 2. 一覧

## 2.1 解消済み（rev2 で確定）

| 項目 | rev2 での確定内容 |
|------|-------------------|
| GBP Secret の実体 | §2.1: JSON 1本（client_id, client_secret, refresh_token） |
| Cloud Run 認証・Scheduler | §2.2: 認証必須、OIDC、run.invoker は Scheduler 用 SA のみ |
| places_provider_map 初期データ | §3.1: 手動INSERT（README/infra に手順） |
| raw_provider_payloads の単位 | §3.4: 店舗×provider×endpoint ごとに1行（endpoint 列あり） |
| 010 の役割 | §9: 雛形（ドキュメント）。実行は bq_writer.py。001/002 は手動適用 |
| VIEW と LATEST の対応 | §5.1, §5.2, §6: 2VIEW を参照して Sheets 全置換 |
| alert_type・しきい値 | §5.3: low_rating, rating_drop, review_surge。閾値は SQL 固定 |
| LATEST の fetched_at | §7.1: 含める（確定） |
| performance_monthly | §4.2, §6: 毎月1日 09:00 JST、別ジョブ推奨 |
| GCS_EXPORT_BUCKET | §8: ENV 必須 |
| テスト配置 | §10: tests/ 配下、pytest tests |
| 実装順 | §12: Phase 1〜5 で定義 |

## 2.2 未解消

**現時点で未解消の疑問点はなし。**  
参照は rev2 に統一し、実装は rev2 に従う。

---

# 3. 今後の確認の目安

- 実装・運用で新たな曖昧点が出た場合は、本ファイルの「2.2 未解消」に項目を追加し、設計書 **rev2** の該当節を確認するか、設計書に追記してから解消済みに移す。
- 設計書を改訂する場合は、ファイル名で rev を上げ（例: _rev3）、本ファイルの「参照する最新版」を更新する。
